import PropTypes from 'prop-types'
import {Link} from 'react-router-dom'

const Modal = ({color, text, onClick, border, p, link}) => {
    return(
        <div className='' style={{backgroundColor: 'red'}}>
            zxc
        </div>
    )
}

Modal.defaultProps = {
 
}

Modal.prototype = {

}

export default Modal
